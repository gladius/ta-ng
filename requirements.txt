fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
langsmith>=0.1          # live LangSmith trace pull (offline/recorded mode needs no key)
anthropic>=0.40         # provider SDK for the paid re-run + judge calls (llm_client)
sqlalchemy>=2.0         # durable shared store (SQLite dev / Postgres prod) — app/services/db.py, store.py
psycopg[binary]>=3.1    # Postgres driver — PROD ONLY; dev uses stdlib sqlite (no install), so skip it in a restricted env

# Run:  pip install -r requirements.txt
#       python -m app.web.server        (http://127.0.0.1:8100)
